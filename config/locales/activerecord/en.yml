en:
  activerecord:
    models:
      user: user
    attributes:
      user:
        email: email
        name: username
        password: password
        password_confirmation: password_confirmation